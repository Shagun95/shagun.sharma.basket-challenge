public enum GameEvent
{
    /// <summary>
    /// Used when the ball is launched though input, parameter is the enum "ShootType"
    /// </summary>
    LAUNCH_BALL,
    /// <summary>
    /// Used to add a score to the player, parameter is an int rappresenting the points to add
    /// </summary>
    ADD_SCORE_TO_PLAYER
}
